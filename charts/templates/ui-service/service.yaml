apiVersion: v1
kind: Service
metadata:
  name: {{ template "infogrep.uiService.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "infogrep.labels" (dict "context" . "component" .Values.UiService.name "name" .Values.UiService.name) | nindent 4 }}
    {{- with .Values.UiService.service.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.UiService.service.annotations }}
  annotations:
    {{- range $key, $value := . }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.UiService.service.type }}
  selector:
    {{- include "infogrep.selectorLabels" (dict "context" . "name" .Values.UiService.name) | nindent 4 }}
  ports:
    - name: {{ .Values.UiService.service.servicePortHttpName }}
      protocol: TCP
      port: {{ .Values.UiService.service.servicePortHttp }}
      targetPort: {{ .Values.UiService.containerPorts.server }}
      {{- if eq .Values.UiService.service.type "NodePort" }}
      nodePort: {{ .Values.UiService.service.nodePortHttp }}
      {{- end }}