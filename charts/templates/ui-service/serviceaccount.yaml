{{- if .Values.UiService.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
automountServiceAccountToken: {{ .Values.UiService.serviceAccount.automountServiceAccountToken }}
metadata:
  name: {{ include "infogrep.uiService.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  {{- with .Values.UiService.serviceAccount.annotations }}
  annotations:
    {{- range $key, $value := . }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
  labels:
    {{- include "infogrep.labels" (dict "context" . "component" .Values.UiService.name "name" .Values.UiService.name) | nindent 4 }}
    {{- with .Values.UiService.serviceAccount.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
