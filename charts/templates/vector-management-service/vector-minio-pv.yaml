kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Values.VectorMinio.persistence.claimName }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "infogrep.labels" (dict "context" . "component" .Values.VectorMinio.persistence.claimName "name" .Values.VectorMinio.persistence.claimName) | nindent 4 }}
{{- with .Values.VectorMinio.persistence.annotations  }}
  annotations:
    {{ toYaml . | indent 4 }}
{{- end }}
spec:
  storageClassName: {{ .Values.VectorMinio.persistence.storageClass }}
  {{- with .Values.VectorMinio.persistence.accessModes }}
  accessModes:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.VectorMinio.persistence.size }}

---

kind: PersistentVolume
apiVersion: v1
metadata:
  name: {{ .Values.VectorMinio.persistence.volumeName }}
  namespace: {{ .Values.namespace }}
{{- with .Values.VectorMinio.persistence.annotations  }}
  annotations:
    {{ toYaml . | indent 4 }}
{{- end }}
  labels:
    {{- include "infogrep.labels" (dict "context" . "component" .Values.VectorMinio.persistence.volumeName "name" .Values.VectorMinio.persistence.volumeName) | nindent 4 }}
spec:
  storageClassName: {{ .Values.VectorMinio.persistence.storageClass }}
  capacity:
    storage: {{ .Values.VectorMinio.persistence.size }}
  {{- with .Values.VectorMinio.persistence.accessModes }}
  accessModes:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- if eq .Values.VectorMinio.persistence.storageClass "manual" }}
  hostPath:
    path: {{ .Values.VectorMinio.persistence.hostPath }}
  {{- else if eq .Values.VectorMinio.persistence.storageClass "local-storage" }}
  local:
    path: {{ .Values.VectorMinio.persistence.hostPath }}
  {{- with .Values.VectorMinio.persistence.nodeAffinity}}
  nodeAffinity:
    {{- toYaml . | nindent 4}}
  {{- end }}
  {{- end }}