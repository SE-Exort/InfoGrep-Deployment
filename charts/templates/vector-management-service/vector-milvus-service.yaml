apiVersion: v1
kind: Service
metadata:
  name: {{ template "infogrep.vectorMilvus.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "infogrep.labels" (dict "context" . "component" .Values.VectorMilvus.name "name" .Values.VectorMilvus.name) | nindent 4 }}
    {{- with .Values.VectorMilvus.service.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.VectorMilvus.service.annotations }}
  annotations:
    {{- range $key, $value := . }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.VectorMilvus.service.type }}
  selector:
    {{- include "infogrep.selectorLabels" (dict "context" . "name" .Values.VectorMilvus.name) | nindent 4 }}
  ports:
    - name: {{ .Values.VectorMilvus.service.servicePortHttpName }}
      protocol: TCP
      port: {{ .Values.VectorMilvus.service.servicePortHttp }}
      targetPort: {{ .Values.VectorMilvus.containerPorts.milvus }}
      {{- if eq .Values.VectorMilvus.service.type "NodePort" }}
      nodePort: {{ .Values.VectorMilvus.service.nodePortHttp }}
      {{- end }}