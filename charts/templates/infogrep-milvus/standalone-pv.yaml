kind: PersistentVolume
apiVersion: v1
metadata:
  name: "{{ .Values.MilvusConfig.persistence.volumeName }}-1"
  namespace: {{ .Values.namespace }}
{{- with .Values.MilvusConfig.persistence.annotations  }}
  annotations:
    {{ toYaml . | indent 4 }}
{{- end }}
  labels:
    {{- include "infogrep.labels" (dict "context" . "component" (printf "%s-%s" .Values.MilvusConfig.persistence.volumeName "1") "name" (printf "%s-%s" .Values.MilvusConfig.persistence.volumeName "1")) | nindent 4 }}
spec:
  storageClassName: {{ .Values.MilvusConfig.persistence.storageClass }}
  persistentVolumeReclaimPolicy: {{ .Values.MilvusConfig.persistence.persistentVolumeReclaimPolicy }}
  capacity:
    storage: {{ .Values.MilvusConfig.persistence.size }}
  {{- with .Values.MilvusConfig.persistence.accessModes }}
  accessModes:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- if eq .Values.MilvusConfig.persistence.storageClass "manual" }}
  hostPath:
    path: "/usr/local/include"
  {{- else if eq .Values.MilvusConfig.persistence.storageClass "local-storage" }}
  local:
    path: "/usr/local/include"
  {{- with .Values.MilvusConfig.persistence.nodeAffinity}}
  nodeAffinity:
    {{- toYaml . | nindent 4}}
  {{- end }}
  {{- end }}
  claimRef:
    namespace: {{ .Values.namespace }}
    name: "{{ template "infogrep.milvus.fullname" . }}-minio"