apiVersion: v1
kind: Service
metadata:
  name: {{ template "infogrep.authPostgres.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "infogrep.labels" (dict "context" . "component" .Values.AuthPostgres.name "name" .Values.AuthPostgres.name) | nindent 4 }}
    {{- with .Values.AuthPostgres.service.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.AuthPostgres.service.annotations }}
  annotations:
    {{- range $key, $value := . }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.AuthPostgres.service.type }}
  selector:
    {{- include "infogrep.selectorLabels" (dict "context" . "name" .Values.AuthPostgres.name) | nindent 4 }}
  ports:
    - name: {{ .Values.AuthPostgres.service.servicePortHttpName }}
      protocol: TCP
      port: {{ .Values.AuthPostgres.service.servicePortHttp }}
      targetPort: {{ .Values.AuthPostgres.containerPorts.server }}
      {{- if eq .Values.AuthPostgres.service.type "NodePort" }}
      nodePort: {{ .Values.AuthPostgres.service.nodePortHttp }}
      {{- end }}