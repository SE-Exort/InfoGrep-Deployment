{{- if .Values.VideoService.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
automountServiceAccountToken: {{ .Values.VideoService.serviceAccount.automountServiceAccountToken }}
metadata:
  name: {{ include "infogrep.videoService.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  {{- with .Values.VideoService.serviceAccount.annotations }}
  annotations:
    {{- range $key, $value := . }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
  labels:
    {{- include "infogrep.labels" (dict "context" . "component" .Values.VideoService.name "name" .Values.VideoService.name) | nindent 4 }}
    {{- with .Values.VideoService.serviceAccount.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
