apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: infogrep-gateway
  namespace: infogrep
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "infogrep.hahafhaha.com"